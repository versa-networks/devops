# =============================================================================
# FortiGate Security Profiles + Policy Configuration
# Purpose: Test data for Fortinet-to-PAN conversion script
#          Equivalent to PAN's profile-setting / url-filtering / virus /
#          spyware / vulnerability profile assignments on security rules
# Sources:  Fortinet official docs, yurisk.info, Fortinet community
# Covers:   Web Filter Profiles, URL Filter Tables, AV Profiles,
#           IPS Sensors, Application Control Lists, DNS Filter,
#           SSL/SSH Inspection, and Firewall Policies that bind them all
# =============================================================================


# -----------------------------------------------------------------------------
# SECTION 1: STATIC URL FILTER TABLES
# Equivalent to PAN: custom URL categories / EDL lists
# These are referenced by ID number inside webfilter profiles
# -----------------------------------------------------------------------------
config webfilter urlfilter
    edit 1
        set name "Blocked_Static_URLs"
        config entries
            edit 1
                set url "*facebook.com"
                set type wildcard
                set action block
                set status enable
            next
            edit 2
                set url "*youtube.com"
                set type wildcard
                set action monitor
                set status enable
            next
            edit 3
                set url "*.corp.example.com"
                set type wildcard
                set action exempt
                set status enable
            next
            edit 4
                set url "espn.com"
                set type simple
                set action exempt
                set exempt fortiguard
                set status enable
            next
            edit 5
                set url "*.msn.com"
                set type wildcard
                set action block
                set status enable
            next
            edit 6
                set url "stackoverflow.com"
                set type regex
                set action block
                set status enable
            next
            edit 7
                set url "badsite.example.com"
                set type simple
                set action block
                set status enable
            next
        end
    next
    edit 2
        set name "Allowed_Whitelist_URLs"
        config entries
            edit 1
                set url "*.windowsupdate.com"
                set type wildcard
                set action exempt
                set status enable
            next
            edit 2
                set url "*.office365.com"
                set type wildcard
                set action exempt
                set status enable
            next
            edit 3
                set url "*.microsoft.com"
                set type wildcard
                set action allow
                set status enable
            next
        end
    next
end


# -----------------------------------------------------------------------------
# SECTION 2: WEB FILTER PROFILES
# Equivalent to PAN: Security Profile > URL Filtering
# Key concept: FortiGuard category IDs replace PAN named categories
#   Common category IDs:
#     2  = Drug Abuse         7  = Malicious Websites
#     26 = Pornography        52 = Social Networking
#     62 = Hacking            72 = Phishing
#     83 = Gambling           86 = Proxy Avoidance
#     14 = Games              17 = Shopping
#     25 = Finance/Banking    30 = News
# Action options: block | monitor | warning | authenticate | exempt
# -----------------------------------------------------------------------------
config webfilter profile
    edit "Shore_Strict_Allow"
        set comment "Strict allow profile - equiv to PAN Shore_Strict_Allow URL profile"
        set feature-set proxy
        config web
            set urlfilter-table 1
            set blacklist enable
            set safe-search url header
            set youtube-restrict strict
            set log-search enable
        end
        config ftgd-wf
            set options error-allow
            config filters
                # --- BLOCK categories (equiv to PAN 'block' list) ---
                edit 1
                    set category 2
                    set action block
                    set log enable
                    set override-replacemsg "URL-block-msg"
                next
                edit 2
                    set category 7
                    set action block
                    set log enable
                next
                edit 3
                    set category 26
                    set action block
                    set log enable
                next
                edit 4
                    set category 62
                    set action block
                    set log enable
                next
                edit 5
                    set category 72
                    set action block
                    set log enable
                next
                edit 6
                    set category 83
                    set action block
                    set log enable
                next
                edit 7
                    set category 86
                    set action block
                    set log enable
                next
                # --- MONITOR/ALERT categories (equiv to PAN 'alert' list) ---
                edit 8
                    set category 14
                    set action monitor
                    set log enable
                next
                edit 9
                    set category 17
                    set action monitor
                    set log enable
                next
                edit 10
                    set category 25
                    set action monitor
                    set log enable
                next
                edit 11
                    set category 30
                    set action monitor
                    set log enable
                next
                edit 12
                    set category 52
                    set action monitor
                    set log enable
                next
                # --- ALLOW categories (allowed without logging) ---
                edit 13
                    set category 98
                    set action allow
                    set log disable
                next
                edit 14
                    set category 99
                    set action allow
                    set log disable
                next
            end
        end
    next
    edit "Block_All_Web"
        set comment "Default-deny web profile - block all except whitelist"
        set feature-set flow
        config web
            set urlfilter-table 2
            set blacklist enable
        end
        config ftgd-wf
            set options error-allow
            config filters
                edit 1
                    set category 0
                    set action block
                    set log enable
                next
            end
        end
    next
    edit "webfilter-demo"
        set comment "NGFW policy mode webfilter demo from Fortinet docs"
        set options block-invalid-url
        config web
            set bword-table 1
            set urlfilter-table 1
            set blacklist enable
        end
    next
    edit "WebProfile_Proxy_Full"
        set comment "Full proxy web filter with override and quota"
        set feature-set proxy
        set ovrd-perm bannedword-override urlfilter-override fortiguard-wf-override contenttype-check-override
        config override
            set ovrd-scope ip
            set ovrd-dur 5m
            set ovrd-user-group "webauthgrp"
            set profile "OverrideAllowAll"
        end
        config web
            set urlfilter-table 1
            set safe-search url header
            set youtube-restrict strict
            set log-search enable
        end
        config ftgd-wf
            set ovrd "83 96 98 99 26 61 86 88 90 23 30 36 37"
            config filters
                edit 1
                    set category 2
                    set action warning
                next
                edit 2
                    set category 7
                    set action warning
                next
                edit 3
                    set category 26
                    set action block
                next
                edit 4
                    set category 72
                    set action block
                next
            end
        end
    next
end


# -----------------------------------------------------------------------------
# SECTION 3: ANTIVIRUS PROFILES
# Equivalent to PAN: Security Profile > Antivirus (virus profile)
# PAN profile: AV_drop_default
# -----------------------------------------------------------------------------
config antivirus profile
    edit "AV_drop_default"
        set comment "Default AV profile - drop on detection, equiv to PAN AV_drop_default"
        set feature-set flow
        set av-virus-log enable
        set av-block-log enable
        config http
            set av-scan block
            set outbreak-prevention block
            set archive-block encrypted corrupt
            set archive-log encrypted corrupt timeout
            set emulator enable
        end
        config ftp
            set av-scan block
            set archive-block encrypted corrupt
            set emulator enable
        end
        config smtp
            set av-scan block
            set archive-block encrypted corrupt
            set emulator enable
        end
        config imap
            set av-scan block
            set archive-block encrypted corrupt
        end
    next
    edit "AV_monitor_default"
        set comment "Monitor only - log but do not block"
        set feature-set flow
        set av-virus-log enable
        config http
            set av-scan monitor
            set outbreak-prevention monitor
        end
        config ftp
            set av-scan monitor
        end
    next
    edit "new-av-profile"
        set comment "Proxy mode AV with emulator - from Fortinet docs example"
        set feature-set proxy
        set mobile-malware-db enable
        set av-virus-log enable
        set av-block-log enable
        set extended-log disable
        set scan-mode default
        config http
            set emulator enable
            set outbreak-prevention disabled
        end
    next
end


# -----------------------------------------------------------------------------
# SECTION 4: IPS SENSORS
# Equivalent to PAN: Security Profile > Vulnerability Protection (IPS)
# PAN profile: IPS_drop_default
# -----------------------------------------------------------------------------
config ips sensor
    edit "IPS_drop_default"
        set comment "Default IPS sensor - drop on critical/high, equiv to PAN IPS_drop_default"
        config entries
            edit 1
                set rule 0
                set severity critical high
                set action drop
                set log enable
                set log-packet enable
            next
            edit 2
                set rule 0
                set severity medium
                set action reset
                set log enable
            next
            edit 3
                set rule 0
                set severity low info
                set action pass
                set log disable
            next
        end
    next
    edit "IPS_monitor_all"
        set comment "Monitor all - log but no blocking"
        config entries
            edit 1
                set rule 0
                set severity critical high medium low info
                set action pass
                set log enable
            next
        end
    next
    edit "IPS_protect_servers"
        set comment "Aggressive IPS for server protection"
        config entries
            edit 1
                set rule 0
                set severity critical high medium
                set action drop
                set log enable
                set log-packet enable
            next
        end
    next
end


# -----------------------------------------------------------------------------
# SECTION 5: APPLICATION CONTROL (APP SENSOR / LIST)
# Equivalent to PAN: application list in security rules
# FortiGate uses application categories and specific app signatures
# -----------------------------------------------------------------------------
config application list
    edit "App_Control_Standard"
        set comment "Standard app control - block high-risk, monitor others"
        config entries
            edit 1
                set category 2
                set action block
                set log enable
            next
            edit 2
                set category 6
                set action block
                set log enable
            next
            edit 3
                set application 26806
                set action block
                set log enable
            next
            edit 4
                set application 16492
                set action block
                set log enable
            next
            edit 5
                set application 38063
                set action block
                set log enable
            nex5
        end
    next
    edit "App_Control_Strict"
        set comment "Strict app control for high security zones"
        set unknown-application-action block
        config entries
            edit 1
                set category 2 6 25
                set action block
                set log enable
            next
            edit 2
                set application 33
                set action monitor
                set log enable
            next
            edit 3
                set application 15832
                set action monitor
                set log enable
            next
            edit 4
                set application 23813
                set action monitor
                set log enable
            next
            edit 5
                set application 38990
                set action monitor
                set log enable
            next
        end
    next
end


# -----------------------------------------------------------------------------
# SECTION 6: DNS FILTER PROFILES
# Equivalent to PAN: DNS Security profile
# -----------------------------------------------------------------------------
config dnsfilter profile
    edit "DNS_Filter_Default"
        set comment "Block malicious domains at DNS level"
        config ftgd-dns
            config filters
                edit 1
                    set category 26
                    set action block
                next
                edit 2
                    set category 72
                    set action block
                next
                edit 3
                    set category 7
                    set action block
                next
            end
        end
        set block-botnet enable
        set safe-search enable
    next
end


# -----------------------------------------------------------------------------
# SECTION 7: SSL/SSH INSPECTION PROFILES
# Equivalent to PAN: SSL Decryption profile
# -----------------------------------------------------------------------------
config firewall ssl-ssh-profile
    edit "deep-inspection"
        set comment "Full SSL deep inspection"
        config https
            set ports 443
            set status deep-inspection
        end
        config ftps
            set ports 990
            set status deep-inspection
        end
        config imaps
            set ports 993
            set status deep-inspection
        end
        set ssl-exempt-categories 72 7
    next
    edit "certificate-inspection"
        set comment "Certificate-only inspection - SNI only, no decrypt"
        config https
            set ports 443
            set status certificate-inspection
        end
    next
    edit "SSL_Inspection"
        set comment "Custom SSL inspection with FGID category exemptions"
        config https
            set ports 443
            set status deep-inspection
        end
        config ssl-exempt
            edit 1
                set fortiguard-category 140
            next
            edit 2
                set fortiguard-category 194
            next
        end
    next
end


# -----------------------------------------------------------------------------
# SECTION 8: USER GROUPS (for user-aware policies)
# Equivalent to PAN: source-user in security rules
# -----------------------------------------------------------------------------
# =============================================================================
# SECTION 8b: LDAP SERVER PROFILE
# Equivalent to PAN: Device > Server Profiles > LDAP
# Referenced by:
#   config user group > set member "<server>"
#   config user group > config match > set server-name "<server>"
#   config user local > set ldap-server "<server>"
# =============================================================================
config user ldap
    edit "LDAP_CORP"
        set server "10.10.0.10"
        set secondary-server "10.10.0.11"
        set cnid "cn"
        set dn "DC=corp,DC=local"
        set type regular
        set username "CN=svc-fortigate-ldap,OU=ServiceAccounts,OU=Corp,DC=corp,DC=local"
        set password ENC XXXXXXXXXXXXXXXXXXXXXXXXXXXX
        set group-member-check group-object
        set group-search-base "OU=User,OU=Corp,DC=corp,DC=local"
        set group-object-filter "(&(objectcategory=group)(member=*))"
        set secure ldaps
        set port 636
    next
end


# =============================================================================
# SECTION 8c: INDIVIDUAL LDAP USERS  (config user local, type ldap)
# Equivalent to PAN: source-user with UPN / sAMAccountName / raw DN
# e.g.  PAN: source-user [ "corp\\dobbermann.martin"  "testuser@corp.local" ]
#       FGT: set users "dobbermann.martin"  (local object that binds to LDAP)
#
# NAMING LEGEND:
#   LEGAL   = [A-Za-z0-9_-] only
#   ILLEGAL = dot / space / @ / \ -- common in firstname.lastname or display-name style
# =============================================================================
config user local

    edit "Jayakumar"
        set type ldap
        set ldap-server "LDAP_CORP"
        set username "CN=Jayakumar,OU=TEST_HQ,OU=TEST_users,OU=TEST_corp,DC=acme,DC=com"
        set comment "ILLEGAL NAME: space -- display name stored as full DN"
    next

    edit "unknown_user"
        set type ldap
        set ldap-server "LDAP_CORP"
        set username "CN=unknown_user,OU=TEST_HQ,OU=TEST_users,OU=TEST_corp,DC=acme,DC=com"
        set comment "ILLEGAL NAME: space -- display name stored as full DN"
    next

    # [LEGAL] underscore-substituted sAMAccountName (sanitised at import)
    edit "dobbermann_martin"
        set type ldap
        set ldap-server "LDAP_CORP"
        set username "dobbermann.martin"
        set comment "LEGAL: dot replaced by underscore at import time"
    next

    # [ILLEGAL] dot -- firstname.lastname kept verbatim
    edit "esser.andreas"
        set type ldap
        set ldap-server "LDAP_CORP"
        set username "esser.andreas"
        set comment "ILLEGAL NAME: dot"
    next

    # [ILLEGAL] dot
    edit "zenglein.georg"
        set type ldap
        set ldap-server "LDAP_CORP"
        set username "zenglein.georg"
        set comment "ILLEGAL NAME: dot"
    next

    # [ILLEGAL] dot
    edit "rossbach.maximilian"
        set type ldap
        set ldap-server "LDAP_CORP"
        set username "rossbach.maximilian"
        set comment "ILLEGAL NAME: dot"
    next

    # [ILLEGAL] dot
    edit "hallidri.refat"
        set type ldap
        set ldap-server "LDAP_CORP"
        set username "acme\hallidri.refat"
        set comment "ILLEGAL NAME: dot"
    next

    # [ILLEGAL] dot
    edit "seifert.ole"
        set type ldap
        set ldap-server "LDAP_CORP"
        set username "seifert.ole@acme.com"
        set comment "ILLEGAL NAME: dot"
    next

    # [ILLEGAL] dot
    edit "perkins.derek"
        set type ldap
        set ldap-server "LDAP_CORP"
        set username "CORP\\perkins.derek"
        set comment "ILLEGAL NAME: dot"
    next

    # [ILLEGAL] dot
    edit "valley.ryan"
        set type ldap
        set ldap-server "LDAP_CORP"
        set username "corp.local/Corp/Users/valley.ryan"
        set comment "ILLEGAL NAME: dot"
    next

    # [ILLEGAL] SPACE -- display-name / CN with spaces
    edit "Oswald Martin"
        set type ldap
        set ldap-server "LDAP_CORP"
        set username "CN=Oswald Martin,OU=Groups,OU=Users,OU=Corp,DC=corp,DC=local"
        set comment "ILLEGAL NAME: space -- display name stored as full DN"
    next

    edit "testuser1"
        set type ldap
        set ldap-server "LDAP_CORP"
        set username "CN=testuser1,OU=research,DC=acme,DC=com"
        set comment "ILLEGAL NAME: space -- display name stored as full DN"
    next

    # [ILLEGAL] SPACE
    edit "Karsten Kubeler"
        set type ldap
        set ldap-server "LDAP_CORP"
        set username "CN=Karsten Kubeler,OU=HQ,OU=Users,OU=Corp,DC=corp,DC=local"
        set comment "ILLEGAL NAME: space -- HQ user identified by full DN"
    next

    # [ILLEGAL] dot
    edit "schroeder.michael"
        set type ldap
        set ldap-server "LDAP_CORP"
        set username "schroeder.michael"
        set comment "ILLEGAL NAME: dot"
    next

    # [ILLEGAL] dot
    edit "wolfram.kristian"
        set type ldap
        set ldap-server "LDAP_CORP"
        set username "wolfram.kristian"
        set comment "ILLEGAL NAME: dot"
    next

end


# =============================================================================
# SECTION 8d: USER GROUPS  (original 3 + extended LDAP-backed groups)
# Equivalent to PAN: source-user LDAP group DN references in security rules
# PAN:  source-user [ "CN=FIM-TECH-Cruises-Division,OU=Groups,OU=Corp,DC=corp,DC=local" ]
# FGT:  set groups "FIM-TECH-Cruises-Division"
#         (group internally maps to that DN via config match)
#
# GROUP TYPES:
#   SINGLE-MAP = 1 FortiGate group name -> 1 LDAP DN
#   MULTI-MAP  = 1 FortiGate group name -> N LDAP DNs  (merged)
#   INLINE-DN  = FortiGate group name IS the raw LDAP DN (FSSO auto-import style)
#
# NAMING LEGEND:
#   LEGAL    = [A-Za-z0-9_-] only
#   ILLEGAL  = space / dot / & / comma / = / ( ) / / + # @
# =============================================================================
config user group

    # ----------------------------------------------------------------
    # Original groups (preserved from base config)
    # ----------------------------------------------------------------

    # [LEGAL] RAVPN internal users group
    edit "RAVPN_intern"
        set member "LDAP_CORP"
        set comment "Internal VPN users - RAVPN_intern"
        config match
            edit 1
                set server-name "LDAP_CORP"
                set group-name "CN=RAVPN_intern,OU=Groups,OU=Users,OU=Corp,DC=corp,DC=local"
            next
        end
    next

    edit "scim_group_1"
        set member "LDAP_CORP"
        set comment "Internal VPN users - RAVPN_intern"
        config match
            edit 1
                set server-name "LDAP_CORP"
                set group-name "CN=scim_group_1,OU=Groups,OU=Users,OU=Corp,DC=corp,DC=local"
            next
        end
    next

    edit "scim_group_2"
        set member "LDAP_CORP"
        set comment "Internal VPN users - RAVPN_intern"
        config match
            edit 1
                set server-name "LDAP_CORP"
                set group-name "CN=scim_group_2,OU=Groups,OU=Users,OU=Corp,DC=corp,DC=local"
            next
        end
    next

    # [LEGAL] Web auth override group (proxy authentication)
    edit "webauthgrp"
        set comment "Web auth override group"
    next

    # [LEGAL] Finance department users
    edit "Finance_Users"
        set member "LDAP_CORP"
        set comment "Finance department user group"
        config match
            edit 1
                set server-name "LDAP_CORP"
                set group-name "CN=FIM-ORG-Finance,OU=Groups,OU=Corp,DC=corp,DC=local"
            next
        end
    next

    # ----------------------------------------------------------------
    # LEGAL single-map groups
    # ----------------------------------------------------------------

    # [LEGAL][SINGLE-MAP] IT System Administration
    edit "FW_IT_SysAdmin"
        set member "LDAP_CORP"
        set comment "IT system administrators"
        config match
            edit 1
                set server-name "LDAP_CORP"
                set group-name "CN=firewall_internal_IT_System_Administration,OU=Groups,OU=Users,OU=Corp,DC=corp,DC=local"
            next
        end
    next

    # [LEGAL][SINGLE-MAP] xMedia admin
    edit "FW_xMedia_Admin"
        set member "LDAP_CORP"
        set comment "xMedia admin users"
        config match
            edit 1
                set server-name "LDAP_CORP"
                set group-name "CN=firewall_internal_xmedia-admin,OU=Groups,OU=Users,OU=Corp,DC=corp,DC=local"
            next
        end
    next

    # [LEGAL][SINGLE-MAP] xMedia users
    edit "FW_xMedia_User"
        set member "LDAP_CORP"
        set comment "xMedia regular users"
        config match
            edit 1
                set server-name "LDAP_CORP"
                set group-name "CN=firewall_internal_xmedia-user,OU=Groups,OU=Users,OU=Corp,DC=corp,DC=local"
            next
        end
    next

    # [LEGAL][SINGLE-MAP] GitLab / DevOps group
    edit "FW_DevOps_GitLab"
        set member "LDAP_CORP"
        set comment "GitLab CI/CD and DevOps users"
        config match
            edit 1
                set server-name "LDAP_CORP"
                set group-name "CN=gitlab,OU=Groups,OU=Users,OU=Corp,DC=corp,DC=local"
            next
        end
    next

    # [LEGAL][SINGLE-MAP] Database administrators
    edit "FW_DB_Admins"
        set member "LDAP_CORP"
        set comment "Database administrators"
        config match
            edit 1
                set server-name "LDAP_CORP"
                set group-name "CN=firewall_internal_it_database_administration,OU=Groups,OU=Users,OU=Corp,DC=corp,DC=local"
            next
        end
    next

    # [LEGAL][SINGLE-MAP] Backup administrators
    edit "FW_BackupAdmins"
        set member "LDAP_CORP"
        set comment "Backup administrators"
        config match
            edit 1
                set server-name "LDAP_CORP"
                set group-name "CN=backupadmins,OU=Groups,OU=Users,OU=Corp,DC=corp,DC=local"
            next
        end
    next

    # [LEGAL][SINGLE-MAP] KPMG external auditors
    edit "FW_KPMG_Auditors"
        set member "LDAP_CORP"
        set comment "KPMG external auditors RAVPN access"
        config match
            edit 1
                set server-name "LDAP_CORP"
                set group-name "CN=ravpn_external_kpmg,OU=Groups,OU=Users,OU=Corp,DC=corp,DC=local"
            next
        end
    next

    # [LEGAL][SINGLE-MAP] Siemens external -- deep Fremdfirmen OU path (8 levels)
    edit "FW_Siemens_External"
        set member "LDAP_CORP"
        set comment "Siemens external vendor -- 8-level Fremdfirmen OU path in DN"
        config match
            edit 1
                set server-name "LDAP_CORP"
                set group-name "CN=webvpn_external_siemens,OU=Siemens,OU=Fremdfirmen,OU=Rostock,OU=Corp,OU=allgemein,OU=Users,OU=Corp,DC=corp,DC=local"
            next
        end
    next

    # [LEGAL][SINGLE-MAP] Fidelio UAT external
    edit "FW_Fidelio_UAT"
        set member "LDAP_CORP"
        set comment "Fidelio UAT external users"
        config match
            edit 1
                set server-name "LDAP_CORP"
                set group-name "CN=firewall_external_fidelio-UAT,OU=Groups,OU=Users,OU=Corp,DC=corp,DC=local"
            next
        end
    next

    # [LEGAL][SINGLE-MAP] OrcaAI external RAVPN -- Fremdfirmen OU
    edit "FW_OrcaAI_RAVPN"
        set member "LDAP_CORP"
        set comment "OrcaAI external vendor RAVPN"
        config match
            edit 1
                set server-name "LDAP_CORP"
                set group-name "CN=ravpn_external_orcaai,OU=orcaai,OU=Fremdfirmen,OU=Rostock,OU=Corp,OU=allgemein,OU=Users,OU=Corp,DC=corp,DC=local"
            next
        end
    next

    # ----------------------------------------------------------------
    # LEGAL multi-map (multiple LDAP DNs merged into one FG group)
    # ----------------------------------------------------------------

    # [LEGAL][MULTI-MAP] Network + IT Security (2 LDAP groups merged)
    edit "FW_NetworkAdmins"
        set member "LDAP_CORP"
        set comment "Network + IT security admins: NetworkAdmin + IT_Security merged"
        config match
            edit 1
                set server-name "LDAP_CORP"
                set group-name "CN=NetworkAdmin,OU=Groups,OU=Users,OU=Corp,DC=corp,DC=local"
            next
            edit 2
                set server-name "LDAP_CORP"
                set group-name "CN=IT_Security,OU=Groups,OU=Users,OU=Corp,DC=corp,DC=local"
            next
        end
    next

    # [LEGAL][MULTI-MAP] External vendor RAVPN groups -- replaces prior multi-map group
    # 4 vendor RAVPN LDAP groups merged into one FortiGate group for policy simplification
    edit "FW_RemoteVendors_All"
        set member "LDAP_CORP"
        set comment "Remote vendor RAVPN users -- danelec, gmis, jbt, kyndryl merged"
        config match
            edit 1
                set server-name "LDAP_CORP"
                set group-name "CN=ravpn_external_danelec,OU=RAVPN,OU=Groups,OU=Corp,DC=corp,DC=local"
            next
            edit 2
                set server-name "LDAP_CORP"
                set group-name "CN=ravpn_external_gmis,OU=Groups,OU=Corp,DC=corp,DC=local"
            next
            edit 3
                set server-name "LDAP_CORP"
                set group-name "CN=ravpn_external_jbt,OU=Groups,OU=Corp,DC=corp,DC=local"
            next
            edit 4
                set server-name "LDAP_CORP"
                set group-name "CN=ravpn_external_kyndryl,OU=Kyndryl,OU=Vendors,OU=Office,OU=Corp,OU=allgemein,OU=Users,OU=Corp,DC=corp,DC=local"
            next
        end
    next


    # [LEGAL][MULTI-MAP] Finance combined -- Accountancy + FIM-ORG Finance (2 DNs)
    edit "FW_Finance_External"
        set member "LDAP_CORP"
        set comment "Finance external: Accountancy_Finance + FIM-ORG-Finance merged"
        config match
            edit 1
                set server-name "LDAP_CORP"
                set group-name "CN=Acountancy_Finance,OU=Groups,OU=Users,OU=Corp,DC=corp,DC=local"
            next
            edit 2
                set server-name "LDAP_CORP"
                set group-name "CN=FIM-ORG-Finance,OU=Groups,OU=Corp,DC=corp,DC=local"
            next
        end
    next

    # ----------------------------------------------------------------
    # ILLEGAL-NAME single-map (name copied verbatim from LDAP CN)
    # ----------------------------------------------------------------

    # [ILLEGAL][SINGLE-MAP] SPACE: "FIM-TECH-Cruises-Division"
    edit "FIM-TECH-Cruises-Division"
        set member "LDAP_CORP"
        set comment "ILLEGAL NAME: space"
        config match
            edit 1
                set server-name "LDAP_CORP"
                set group-name "CN=FIM-TECH-Cruises-Division,OU=Groups,OU=Users,OU=Corp,DC=corp,DC=local"
            next
        end
    next

    # [ILLEGAL][SINGLE-MAP] SPACE: "FIM-TECH-CustomerCenter"
    edit "FIM-TECH-CustomerCenter"
        set member "LDAP_CORP"
        set comment "ILLEGAL NAME: space"
        config match
            edit 1
                set server-name "LDAP_CORP"
                set group-name "CN=FIM-TECH-CustomerCenter,OU=Groups,OU=Users,OU=Corp,DC=corp,DC=local"
            next
        end
    next

    # [ILLEGAL][SINGLE-MAP] SPACES + DASH: "CMG - DISTLIST RAAS"
    edit "CMG - DISTLIST RAAS"
        set member "LDAP_CORP"
        set comment "ILLEGAL NAME: spaces and dash"
        config match
            edit 1
                set server-name "LDAP_CORP"
                set group-name "CN=CMG - DISTLIST RAAS,OU=Groups,OU=Users,OU=Corp,DC=corp,DC=local"
            next
        end
    next

    # [ILLEGAL][SINGLE-MAP] SPACE: "FIM-ORG-Yield Management"
    edit "FIM-ORG-Yield Management"
        set member "LDAP_CORP"
        set comment "ILLEGAL NAME: space"
        config match
            edit 1
                set server-name "LDAP_CORP"
                set group-name "CN=FIM-ORG-Yield Management,OU=Groups,OU=Users,OU=Corp,DC=corp,DC=local"
            next
        end
    next

    # [ILLEGAL][SINGLE-MAP] DOT + SPACE: "v.IT-System Admins"
    edit "v.IT-System Admins"
        set member "LDAP_CORP"
        set comment "ILLEGAL NAME: dot and space"
        config match
            edit 1
                set server-name "LDAP_CORP"
                set group-name "CN=v.IT-System Admins,OU=Groups,OU=O365,OU=HQ,OU=Users,OU=Corp,DC=corp,DC=local"
            next
        end
    next

    # [ILLEGAL][SINGLE-MAP] SPACE: "FIM-TECH-Maritime-Division"
    edit "FIM-TECH-Maritime-Division"
        set member "LDAP_CORP"
        set comment "ILLEGAL NAME: space"
        config match
            edit 1
                set server-name "LDAP_CORP"
                set group-name "CN=FIM-TECH-Maritime-Division,OU=Groups,OU=Users,OU=Corp,DC=corp,DC=local"
            next
        end
    next

    # [ILLEGAL][SINGLE-MAP] SPACE: "FIM-TECH-Entertainment-Group"
    edit "FIM-TECH-Entertainment-Group"
        set member "LDAP_CORP"
        set comment "ILLEGAL NAME: space"
        config match
            edit 1
                set server-name "LDAP_CORP"
                set group-name "CN=FIM-TECH-Entertainment-Group,OU=Groups,OU=Users,OU=Corp,DC=corp,DC=local"
            next
        end
    next

    # [ILLEGAL][SINGLE-MAP] SPACE (lowercase): "it security"
    edit "it security"
        set member "LDAP_CORP"
        set comment "ILLEGAL NAME: space -- lowercase, ServiceNow OU"
        config match
            edit 1
                set server-name "LDAP_CORP"
                set group-name "CN=it security,OU=servicenow,OU=groups,OU=users,OU=corp,DC=corp,DC=local"
            next
        end
    next

    # ----------------------------------------------------------------
    # ILLEGAL-NAME multi-map
    # ----------------------------------------------------------------

    # [ILLEGAL][MULTI-MAP] "IT Enterprise Applications" -- space, 2 DNs
    edit "IT Enterprise Applications"
        set member "LDAP_CORP"
        set comment "ILLEGAL NAME: spaces -- Groups OU + ServiceNow OU merged"
        config match
            edit 1
                set server-name "LDAP_CORP"
                set group-name "CN=IT Enterprise Applications,OU=Groups,OU=Users,OU=Corp,DC=corp,DC=local"
            next
            edit 2
                set server-name "LDAP_CORP"
                set group-name "CN=IT Enterprise Applications,OU=ServiceNow,OU=Groups,OU=Users,OU=Corp,DC=corp,DC=local"
            next
        end
    next

    # [ILLEGAL][MULTI-MAP] "FIM-ORG-CMG Fleet Operations Center" -- spaces, 2 DNs
    edit "FIM-ORG-CMG Fleet Operations Center"
        set member "LDAP_CORP"
        set comment "ILLEGAL NAME: spaces -- CMG Fleet Ops + IT Fleet Ops merged"
        config match
            edit 1
                set server-name "LDAP_CORP"
                set group-name "CN=FIM-ORG-CMG Fleet Operations Center,OU=Groups,OU=Users,OU=Corp,DC=corp,DC=local"
            next
            edit 2
                set server-name "LDAP_CORP"
                set group-name "CN=firewall_external_IT_Fleet_Ops,OU=Groups,OU=Users,OU=Corp,DC=corp,DC=local"
            next
        end
    next

    # ----------------------------------------------------------------
    # INLINE-DN groups (group name = raw LDAP DN; commas, =, spaces)
    # ----------------------------------------------------------------

    # [ILLEGAL][INLINE-DN] Raw DN -- space in CN value
    edit "CN=FIM-ORG-IT Business,OU=Groups,OU=Corp,DC=corp,DC=local"
        set member "LDAP_CORP"
        set comment "ILLEGAL NAME: raw LDAP DN -- commas, equals, spaces"
        config match
            edit 1
                set server-name "LDAP_CORP"
                set group-name "CN=FIM-ORG-IT Business,OU=Groups,OU=Corp,DC=corp,DC=local"
            next
        end
    next

    # [ILLEGAL][INLINE-DN] Raw DN -- OU has spaces + GmbH abbreviation
    edit "CN=FIM-ORG-Yield Management,OU=TEST_Groups,OU=TEST_users,OU=TEST_corp,DC=acme,DC=com"
        set member "LDAP_CORP"
        set comment "ILLEGAL NAME: raw DN -- OU value has spaces and GmbH"
        config match
            edit 1
                set server-name "LDAP_CORP"
                set group-name "CN=FIM-ORG-Yield Management,OU=TEST_Groups,OU=TEST_users,OU=TEST_corp,DC=acme,DC=com"
            next
        end
    next

    # [ILLEGAL][INLINE-DN] Raw DN -- OU has dash, spaces, dots, e.K abbreviation
    edit "CN=webvpn_external_itron,OU=itron - Industrie-Elektronik Martin Willers e.K,OU=Fremdfirmen,OU=Rostock,OU=Corp,OU=allgemein,OU=Users,OU=Corp,DC=corp,DC=local"
        set member "LDAP_CORP"
        set comment "ILLEGAL NAME: raw DN -- OU has dash, spaces, dots, period"
        config match
            edit 1
                set server-name "LDAP_CORP"
                set group-name "CN=webvpn_external_itron,OU=itron - Industrie-Elektronik Martin Willers e.K,OU=Fremdfirmen,OU=Rostock,OU=Corp,OU=allgemein,OU=Users,OU=Corp,DC=corp,DC=local"
            next
        end
    next

end


# -----------------------------------------------------------------------------
# SECTION 9: LOG SETTINGS
# Equivalent to PAN: log-setting (Panorama)
# -----------------------------------------------------------------------------
config log fortianalyzer setting
    set status enable
    set server "192.168.100.10"
    set source-ip ''
    set upload-option realtime
    set reliable enable
end

config log setting
    set fwpolicy-implicit-log enable
    set local-in-allow enable
    set local-in-deny-unicast enable
end


# -----------------------------------------------------------------------------

# =============================================================================
# SECTION 10a: FIREWALL ADDRESS OBJECTS
# Equivalent to PAN: address objects (ip-netmask, fqdn, ip-range)
#
# Object naming strategy for Versa-sanitisation testing:
#   Versa only allows [A-Za-z 0-9 _ -] in object names.
#   Characters intentionally embedded below (must be replaced/stripped):
#     .  (dot)          space          (  )  (parentheses)
#     &  (ampersand)    @  (at-sign)   /  (slash)
#     #  (hash)         +  (plus)      :  (colon)
#     [  ]  (brackets)
#
# USED-BY cross-reference (U = used by a policy, G = used by a group, O = orphan)
# =============================================================================
config firewall address

    # ------------------------------------------------------------------
    # A) Subnet objects -- objects referenced directly by policies
    # ------------------------------------------------------------------

    # [U] ravpn-client-net -- used by Policy 1, 10, 13 srcaddr
    edit "unused-address1"
        set type ipmask
        set subnet 10.200.0.0 255.255.255.0
        set comment "RAVPN client pool - PAN equiv ravpn-client-net"
    next
    
    edit "ravpn-client-net"
        set type ipmask
        set subnet 10.200.0.0 255.255.255.0
        set comment "RAVPN client pool - PAN equiv ravpn-client-net"
    next

    # [U] PUBLIC-NETWORKS -- used by Policy 1, 10 dstaddr
    edit "PUBLIC-NETWORKS"
        set type ipmask
        set subnet 0.0.0.0 0.0.0.0
        set comment "Default route catch-all for internet - PAN equiv PUBLIC-NETWORKS"
    next

    # [U] LAN -- used by Policy 2, 14 srcaddr
    edit "LAN"
        set type ipmask
        set subnet 192.168.1.0 255.255.255.0
        set comment "Corporate LAN segment"
    next

    # [U] WebServer_DMZ -- used by Policy 3 srcaddr
    edit "WebServer_DMZ"
        set type ipmask
        set subnet 172.16.10.10 255.255.255.255
        set comment "Primary DMZ web server host"
    next

    # [G] Internal_WebServer -- used by Policy 7 dstaddr; also member of Internal_Networks_Grp
    edit "Internal_WebServer"
        set type ipmask
        set subnet 10.10.10.80 255.255.255.255
        set comment "Internal web server - PAN equiv object by same name"
    next

    # [G] Server_Farm_Primary -- used in group Prod_Server_Groups
    edit "Server_Farm_Primary"
        set type ipmask
        set subnet 10.10.20.0 255.255.255.0
        set comment "Primary production server farm"
    next

    # [G] Server_Farm_Secondary -- used in group Prod_Server_Groups
    edit "Server_Farm_Secondary"
        set type ipmask
        set subnet 10.10.21.0 255.255.255.0
        set comment "Secondary production server farm"
    next

    # [G] DC_Rostock_Primary -- used in group DC_Servers_All
    edit "DC_Rostock_Primary"
        set type ipmask
        set subnet 10.10.1.1 255.255.255.255
        set comment "Domain controller - Rostock primary"
    next

    # [G] DC_Hamburg_Secondary -- used in group DC_Servers_All
    edit "DC_Hamburg_Secondary"
        set type ipmask
        set subnet 10.10.2.1 255.255.255.255
        set comment "Domain controller - Hamburg secondary"
    next

    # [G] Mgmt_Network -- used in group Mgmt_and_Backup_Grp
    edit "Mgmt_Network"
        set type ipmask
        set subnet 10.99.0.0 255.255.0.0
        set comment "Out-of-band management network"
    next

    # [G] Backup_Server_01 -- used in group Mgmt_and_Backup_Grp
    edit "Backup_Server_01"
        set type ipmask
        set subnet 10.10.50.10 255.255.255.255
        set comment "Primary backup server"
    next

    # [G] Backup_Server_02 -- used in group Mgmt_and_Backup_Grp
    edit "Backup_Server_02"
        set type ipmask
        set subnet 10.10.50.11 255.255.255.255
        set comment "Secondary backup server"
    next

    # ------------------------------------------------------------------
    # B) Subnet objects -- Versa-ILLEGAL names (dots, parens, etc.)
    # ------------------------------------------------------------------

    # [G] Name has DOT -- used in group Internet_DMZ_Grp
    edit "Server.Farm.Production"
        set type ipmask
        set subnet 10.10.30.0 255.255.255.0
        set comment "ILLEGAL NAME: dots must be replaced -- prod farm segment"
    next

    # [G] Name has SPACE + AMPERSAND -- used in group Finance_and_HR_Grp
    edit "HR & Payroll Servers"
        set type ipmask
        set subnet 10.10.40.0 255.255.255.0
        set comment "ILLEGAL NAME: space and ampersand -- HR payroll segment"
    next

    # [G] Name has PARENTHESES -- used in group Internet_DMZ_Grp
    edit "DMZ-Servers(Primary)"
        set type ipmask
        set subnet 172.16.10.0 255.255.255.0
        set comment "ILLEGAL NAME: parentheses -- primary DMZ block"
    next

    # [G] Name has AT-SIGN -- used in group Finance_and_HR_Grp
    edit "Branch@Office-LAN"
        set type ipmask
        set subnet 192.168.50.0 255.255.255.0
        set comment "ILLEGAL NAME: at-sign -- remote branch LAN"
    next

    # [G] Name has SLASH -- used in group DC_Servers_All
    edit "DC_Rostock/Standby"
        set type ipmask
        set subnet 10.10.1.2 255.255.255.255
        set comment "ILLEGAL NAME: slash -- Rostock standby DC"
    next

    # [O] Name has HASH -- ORPHAN (not used by any policy or group)
    edit "Backup#Servers-Orphan"
        set type ipmask
        set subnet 10.10.51.0 255.255.255.0
        set comment "ILLEGAL NAME: hash -- ORPHAN: not referenced anywhere"
    next

    # [O] Name has PLUS -- ORPHAN
    edit "Finance+Controllers"
        set type ipmask
        set subnet 10.10.41.0 255.255.255.0
        set comment "ILLEGAL NAME: plus sign -- ORPHAN: not referenced anywhere"
    next

    # [O] Name has COLON -- ORPHAN
    edit "Mgmt:Network-Orphan"
        set type ipmask
        set subnet 10.99.1.0 255.255.255.0
        set comment "ILLEGAL NAME: colon -- ORPHAN: not referenced anywhere"
    next

    # ------------------------------------------------------------------
    # C) FQDN address objects
    # ------------------------------------------------------------------

    # [G] Valid name -- used in group Cloud_Services_Grp
    edit "FQDN-corp-example-com"
        set type fqdn
        set fqdn "corp.example.com"
        set comment "Corporate domain FQDN -- valid Versa name"
    next

    # [G] Name has DOT -- used in group Cloud_Services_Grp
    edit "api.vendor.prod"
        set type fqdn
        set fqdn "api.vendor.prod.example.com"
        set comment "ILLEGAL NAME: dots -- vendor API endpoint"
    next

    # [G] Name has DOT -- used in group Cloud_Services_Grp
    edit "sso.company.com"
        set type fqdn
        set fqdn "sso.company.com"
        set comment "ILLEGAL NAME: dots -- SSO/IdP endpoint"
    next

    # [G] Name has AT-SIGN -- used in group Cloud_Services_Grp
    edit "update.service@prod"
        set type fqdn
        set fqdn "updates.prod.example.com"
        set comment "ILLEGAL NAME: at-sign and dots -- update service FQDN"
    next

    # [O] Name has PARENS+DOT -- ORPHAN
    edit "api.vendor(test)"
        set type fqdn
        set fqdn "api.vendor.test.example.com"
        set comment "ILLEGAL NAME: dot+parens -- ORPHAN: test endpoint not referenced"
    next

    # [O] Valid name -- ORPHAN (valid name, simply never used)
    edit "Orphan_FQDN_UpdateSrv"
        set type fqdn
        set fqdn "updates.internal.example.com"
        set comment "ORPHAN: valid name but not referenced by any policy or group"
    next

    # ------------------------------------------------------------------
    # D) IP-Range address objects
    # ------------------------------------------------------------------

    # [U] IP_Range_Finance -- used by Policy 5, 11 srcaddr
    edit "IP_Range_Finance"
        set type iprange
        set start-ip 10.10.40.100
        set end-ip   10.10.40.200
        set comment "Finance user DHCP pool range"
    next

    # [U] Internal_Networks -- used by Policy 8 srcaddr
    edit "Internal_Networks"
        set type iprange
        set start-ip 10.0.0.1
        set end-ip   10.255.255.254
        set comment "Entire internal RFC-1918 block used as range for policy match"
    next

    # [G] Name has BRACKETS+SPACE -- used in group Finance_and_HR_Grp
    edit "Range Finance [Prod]"
        set type iprange
        set start-ip 10.10.40.201
        set end-ip   10.10.40.254
        set comment "ILLEGAL NAME: brackets+space -- finance prod DHCP overflow range"
    next

    # [G] Name has SLASH -- used in group Internet_DMZ_Grp
    edit "DHCP-Pool/Guest"
        set type iprange
        set start-ip 192.168.100.10
        set end-ip   192.168.100.250
        set comment "ILLEGAL NAME: slash -- guest wifi DHCP pool"
    next

    # [G] Name has PLUS+SPACE -- used in group Mgmt_and_Backup_Grp
    edit "IP Range+DMZ Servers"
        set type iprange
        set start-ip 172.16.10.1
        set end-ip   172.16.10.50
        set comment "ILLEGAL NAME: plus+space -- DMZ server allocated range"
    next

    # [O] Name has PARENS -- ORPHAN
    edit "Unused(Test)Range"
        set type iprange
        set start-ip 172.31.0.1
        set end-ip   172.31.0.50
        set comment "ILLEGAL NAME: parens -- ORPHAN: test range not used anywhere"
    next

    # [O] Valid name -- ORPHAN
    edit "Orphan_Subnet_01"
        set type ipmask
        set subnet 10.10.99.0 255.255.255.0
        set comment "ORPHAN: valid name, not referenced anywhere -- legacy segment"
    next

    # [O] Name has DOT -- ORPHAN
    edit "legacy.server.old"
        set type ipmask
        set subnet 10.10.98.5 255.255.255.255
        set comment "ILLEGAL NAME: dots -- ORPHAN: decommissioned legacy host"
    next

    # Geo-object (referenced by Policy 9 -- deny rule)
    edit "GEO_US"
        set type geography
        set country "US"
        set comment "US geo-block -- referenced by deny policy"
    next

end


# =============================================================================
# SECTION 10b: FIREWALL ADDRESS GROUPS
# Equivalent to PAN: address-group with static members
#
# Group hierarchy:
#   Leaf groups  -> contain only address objects
#   Parent groups -> contain address objects AND other groups (group-in-group)
#
# USED-BY legend:  [U] = used by a policy   [G] = used by a parent group
#                  [O] = orphan group (not referenced anywhere)
# =============================================================================
config firewall addrgrp

    # ------------------------------------------------------------------
    # LEAF groups -- contain only address objects
    # ------------------------------------------------------------------

    # [G] Prod_Server_Groups -- used inside parent group All_Servers_Grp
    edit "unused-address-group1"
        set member "Server_Farm_Primary" "Server_Farm_Secondary" "Internal_WebServer"
        set comment "All production servers -- leaf group, child of All_Servers_Grp"
    next
    
    
    edit "Prod_Server_Groups"
        set member "Server_Farm_Primary" "Server_Farm_Secondary" "Internal_WebServer"
        set comment "All production servers -- leaf group, child of All_Servers_Grp"
    next

    # [G] DC_Servers_All -- used inside parent group All_Servers_Grp
    edit "DC_Servers_All"
        set member "DC_Rostock_Primary" "DC_Hamburg_Secondary" "DC_Rostock/Standby"
        set comment "All domain controllers incl object with illegal slash char"
    next

    # [G] Mgmt_and_Backup_Grp -- used inside parent group All_Servers_Grp
    edit "Mgmt_and_Backup_Grp"
        set member "Mgmt_Network" "Backup_Server_01" "Backup_Server_02" "IP Range+DMZ Servers"
        set comment "Management and backup infrastructure -- leaf group with illegal-name member"
    next

    # [G] Finance_and_HR_Grp -- used inside parent group Internal_Office_Grp
    #     name has AMPERSAND -- ILLEGAL for Versa
    edit "Finance & HR Group"
        set member "IP_Range_Finance" "HR & Payroll Servers" "Branch@Office-LAN" "Range Finance [Prod]"
        set comment "ILLEGAL NAME: ampersand+space -- finance and HR combined group"
    next

    # [G] Internet_DMZ_Grp -- used inside parent group DMZ_and_Internet_Grp
    #     name has DOT -- ILLEGAL for Versa
    edit "Internet.DMZ.Group"
        set member "Server.Farm.Production" "DMZ-Servers(Primary)" "DHCP-Pool/Guest"
        set comment "ILLEGAL NAME: dots -- internet-facing DMZ hosts"
    next

    # [G] Cloud_Services_Grp -- used inside parent group External_Resources_Grp
    edit "Cloud_Services_Grp"
        set member "FQDN-corp-example-com" "api.vendor.prod" "sso.company.com" "update.service@prod"
        set comment "Cloud and SaaS FQDN objects - members include illegal-name objects"
    next

    # [O] Orphan leaf group -- VALID name but never referenced anywhere
    edit "Legacy_Servers_Orphan"
        set member "Orphan_Subnet_01" "legacy.server.old"
        set comment "ORPHAN leaf group -- not used by any policy or parent group"
    next

    # [O] Orphan leaf group -- ILLEGAL name AND orphan
    #     name has HASH+PARENS -- ILLEGAL for Versa
    edit "Unused#Servers(Lab)"
        set member "Orphan_Subnet_01" "Backup#Servers-Orphan"
        set comment "ILLEGAL NAME: hash+parens -- ORPHAN: lab group not used anywhere"
    next

    # ------------------------------------------------------------------
    # PARENT groups (group-in-group) -- contain both objects and groups
    # ------------------------------------------------------------------

    # [U] All_Servers_Grp -- used by Policy 3 (DMZ_Outbound_Strict srcaddr alternative)
    #     Contains: 2 address objects + 3 child groups  (group-in-group x3)
    edit "All_Servers_Grp"
        set member "WebServer_DMZ" "Internal_WebServer" \
                   "Prod_Server_Groups" "DC_Servers_All" "Mgmt_and_Backup_Grp"
        set comment "Parent group: flat objects + 3 leaf groups -- group-in-group depth=2"
    next

    # [U] Internal_Office_Grp -- used by Policy 8 (Block_Social_Media srcaddr alternative)
    #     name has PARENS -- ILLEGAL for Versa
    #     Contains: 1 address object + 1 child group  (group-in-group)
    edit "Internal_Office(Users)"
        set member "LAN" "Finance & HR Group"
        set comment "ILLEGAL NAME: parens -- parent group: LAN + Finance&HR child group"
    next

    # [U] DMZ_and_Internet_Grp -- used by Policy 7 srcaddr alternative
    #     name has SLASH -- ILLEGAL for Versa
    #     Contains: 1 address object + 1 child group  (group-in-group)
    edit "DMZ/Internet-Group"
        set member "WebServer_DMZ" "Internet.DMZ.Group"
        set comment "ILLEGAL NAME: slash -- parent group: DMZ host + Internet.DMZ child group"
    next

    # [U] External_Resources_Grp -- deep nesting: parent > child > FQDNs
    #     name has PLUS -- ILLEGAL for Versa
    #     Contains: 2 address objects + 1 child group  (group-in-group)
    edit "External+Resources-Grp"
        set member "PUBLIC-NETWORKS" "GEO_US" "Cloud_Services_Grp"
        set comment "ILLEGAL NAME: plus -- parent group: public addrs + cloud FQDN child group"
    next

    # [O] Orphan parent group -- ILLEGAL name, and never referenced by any policy
    #     name has AT+DOT -- ILLEGAL for Versa
    edit "Orphan@Parent.Grp"
        set member "Mgmt_Network" "DC_Servers_All" "Legacy_Servers_Orphan"
        set comment "ILLEGAL NAME: at+dot -- ORPHAN parent group, not used anywhere"
    next

    # [O] Orphan deep-nest -- triple illegal chars, never used
    #     name has BRACKETS+SPACE+AMPERSAND -- ILLEGAL for Versa
    edit "Unused [Nested & Groups]"
        set member "All_Servers_Grp" "Orphan@Parent.Grp"
        set comment "ILLEGAL NAME: brackets+space+ampersand -- ORPHAN deep nested group"
    next

end


# =============================================================================
# SECTION 10c: FIREWALL SERVICE CUSTOM OBJECTS
# Equivalent to PAN: service objects (TCP/UDP with src/dst ports)
#
# FortiGate port syntax:
#   set tcp-portrange <dst>                    -- dst port, any src
#   set tcp-portrange <dst>:<src>              -- dst port, specific src port
#   set tcp-portrange <dst1>-<dst2>:<src1>-<src2>  -- dst range : src range
#   set udp-portrange <dst>                    -- same but UDP
#
# Versa-illegal chars used in names (same set as addresses):
#   .  space  ( )  &  @  /  #  +  :  [ ]
# =============================================================================
config firewall service custom

    # ------------------------------------------------------------------
    # A) TCP services -- single exact port, src+dst specified
    # ------------------------------------------------------------------

    # [U] Used by Policy 2 (LAN_Internet_Filtered) and groups
    edit "Custom_unused_service"
        set protocol TCP
        set tcp-portrange 80
        set comment "Standard HTTP - dst 80, any src"
    next

    edit "Custom_HTTP"
        set protocol TCP
        set tcp-portrange 80
        set comment "Standard HTTP - dst 80, any src"
    next

    # [U] Used by Policy 2 and groups
    edit "Custom_HTTPS"
        set protocol TCP
        set tcp-portrange 443
        set comment "Standard HTTPS - dst 443, any src"
    next

    # [U] Used by Policy 3 (DMZ_Outbound_Strict)
    edit "Custom_DNS"
        set protocol TCP UDP
        set tcp-portrange 53
        set udp-portrange 53
        set comment "DNS over TCP and UDP dst 53"
    next
    # [U] Custom definition added -- maps Fortinet predefined SMTP (TCP 25)
    edit "Custom_SMTP"
        set protocol TCP
        set tcp-portrange 25
        set comment "Custom SMTP - dst 25, any src"
    next

    # [U] Custom definition added -- maps Fortinet predefined RDP (TCP 3389)
    edit "Custom_RDP"
        set protocol TCP
        set tcp-portrange 3389:1024-65535
        set comment "Custom RDP - Terminal Services dst 3389 src ephemeral"
    next


    # [G] Name has DOT -- used in service group Web_Services_Grp
    #     src port explicitly constrained to ephemeral range
    edit "Custom.HTTP-8080"
        set protocol TCP
        set tcp-portrange 8080:1024-65535
        set comment "ILLEGAL NAME: dot -- alt HTTP port 8080, ephemeral src"
    next

    # [G] Name has PLUS -- used in service group DB_Services_Grp
    #     Oracle listener: dst 1521, src ephemeral
    edit "Oracle DB Listener"
        set protocol TCP
        set tcp-src-port 1024-65535
        set comment "ILLEGAL NAME: plus -- Oracle TNS listener dst 1521 src ephemeral"
    next

    # [G] Name has SLASH -- used in service group DB_Services_Grp
    #     SAP HANA index server
    edit "SAP/HANA-Port"
        set protocol TCP
        set tcp-portrange 1024-65535
        set comment "ILLEGAL NAME: slash -- SAP HANA index server dst 30015 src ephemeral"
    next

    # [G] Name has AT -- used in service group IoT_Services_Grp
    #     MQTT broker: dst 1883, any src
    edit "MQTT@IoT"
        set protocol TCP
        set tcp-portrange 1883
        set comment "ILLEGAL NAME: at-sign -- MQTT broker dst 1883 any src"
    next

    # [G] Name has PARENS -- used in service group Remote_Access_Grp
    #     RDP: dst 3389, src ephemeral
    edit "Custom RDP(TS)"
        set protocol TCP
        set tcp-portrange 3389:1024-65535
        set comment "ILLEGAL NAME: parens+space -- RDP Terminal Services dst 3389 src ephemeral"
    next

    # [G] Name has COLON+SPACE -- used in service group Web_Services_Grp
    #     HTTPS alt: dst 8443, any src
    edit "App:Server 8443"
        set protocol TCP
        set tcp-portrange 8443
        set comment "ILLEGAL NAME: colon+space -- app-server HTTPS alt port dst 8443"
    next

    # [G] Name has HASH -- used in service group Mgmt_Services_Grp
    #     SNMP over UDP dst 161, TCP dst 162 (trap)
    edit "SNMP#Monitor"
        set protocol TCP UDP
        set tcp-portrange 162-262
        set tcp-src-port 1024-65535
        set comment "ILLEGAL NAME: hash -- SNMP dst 161/UDP + trap 162/TCP"
    next

    # [G] Name has BRACKETS -- used in service group Mgmt_Services_Grp
    #     SSH: dst 22, src 1024-65535
    edit "SSH [Admin]"
        set protocol TCP UDP
        set tcp-portrange 22-222
        set udp-portrange 555-655
        set comment "ILLEGAL NAME: brackets+space -- SSH admin access dst 22 src ephemeral"
    next

    # [G] Name has AMPERSAND+SPACE -- used in service group Mail_Services_Grp
    #     SMTP+SUBMISSION: both dst ports, any src
    edit "SMTP & Submission"
        set protocol TCP
        set tcp-portrange 25 587
        set comment "ILLEGAL NAME: ampersand+space -- SMTP dst 25 and submission dst 587"
    next

    # [G] Name has DOT -- used in service group Mail_Services_Grp
    #     IMAPS: dst 993, src ephemeral
    edit "IMAP.Secure"
        set protocol TCP
        set tcp-portrange 993:1024-65535
        set comment "ILLEGAL NAME: dot -- IMAPS dst 993 src ephemeral"
    next

    # ------------------------------------------------------------------
    # B) TCP/UDP services -- port RANGES for both src and dst
    # ------------------------------------------------------------------

    # [G] Name has BRACKETS+DOT -- used in group Web_Services_Grp
    #     High ephemeral TCP ports
    edit "HighPorts Range[TCP]"
        set protocol TCP
        set tcp-portrange 49152-65535:1024-65535
        set comment "ILLEGAL NAME: brackets+space -- high ephemeral port range dst+src both ranges"
    next

    # [G] Name has PLUS+SPACE -- used in group Mgmt_Services_Grp
    #     Management web UIs: dst range 8000-9000, src ephemeral
    edit "Mgmt Ports+Range"
        set protocol UDP TCP
        set udp-portrange 1024-65535
        set tcp-src-port 1111-2222
        set comment "ILLEGAL NAME: plus+space -- management web UI port range dst 8000-9000"
    next

    # [G] Name has SLASH -- used in group IoT_Services_Grp
    #     Traceroute/UDP probes: dst 33434-33534, src 32768-65535
    edit "Dynamic/UDP Range"
        set protocol UDP
        set udp-portrange 33434-33534
        set comment "ILLEGAL NAME: slash -- UDP traceroute probe range dst+src ranges"
    next

    # [G] Name has PLUS -- used in group IoT_Services_Grp
    #     RTP voice media: UDP dst 16384-32767, src 16384-32767
    edit "Voice RTP+Ports"
        set protocol UDP
        set udp-portrange 16384-32767:16384-32767
        set comment "ILLEGAL NAME: plus -- RTP media streams UDP dst+src symmetric ranges"
    next

    # [G] Name has AT+SPACE -- used in group DB_Services_Grp
    #     MSSQL: dst 1433-1434, src ephemeral
    edit "MSSQL @ DB Range"
        set protocol UDP
        set udp-portrange 1024-65535
        set udp-src-port 444-644
        set comment "ILLEGAL NAME: at+space -- MS SQL Server dst range 1433-1434 src ephemeral"
    next

    # [G] Name has PARENS+SPACE -- used in group Remote_Access_Grp
    #     VNC range: dst 5900-5909, src ephemeral
    edit "VNC Range(Multi)"
        set protocol TCP
        set tcp-portrange 5900-5909:1024-65535
        set comment "ILLEGAL NAME: parens+space -- VNC display range dst 5900-5909 src ephemeral"
    next

    # [G] Name has DOT+SPACE -- used in group Remote_Access_Grp
    #     PCoIP (VMware Horizon): UDP dst 4172, TCP dst 4172, src ephemeral
    edit "PCoIP.VMware Horizon"
        set protocol TCP UDP
        set tcp-portrange 4172:1024-65535
        set udp-portrange 4172:1024-65535
        set comment "ILLEGAL NAME: dot+space -- VMware Horizon PCoIP dst 4172 TCP+UDP src ephemeral"
    next

    # ------------------------------------------------------------------
    # C) Orphan service objects -- not referenced by any group or policy
    # ------------------------------------------------------------------

    # [O] Valid name, unused
    edit "Orphan_TCP_Service"
        set protocol TCP
        set tcp-portrange 9999
        set comment "ORPHAN: valid name -- legacy monitoring port, not used anywhere"
    next

    # [O] Name has DOT -- ILLEGAL + orphan
    edit "legacy.app.port"
        set protocol TCP
        set tcp-portrange 7777:1024-65535
        set comment "ILLEGAL NAME: dots -- ORPHAN: legacy application port, not referenced"
    next

    # [O] Name has HASH+BRACKETS -- ILLEGAL + orphan
    edit "Unused#Port[Range]"
        set protocol TCP
        set tcp-portrange 6000-6010:1024-65535
        set comment "ILLEGAL NAME: hash+brackets -- ORPHAN: not referenced by any group or policy"
    next

    # [O] Name has COLON -- ILLEGAL + orphan, UDP range
    edit "Test:UDP-Range"
        set protocol UDP
        set udp-portrange 10000-11000:32768-65535
        set comment "ILLEGAL NAME: colon -- ORPHAN: test UDP range, not referenced"
    next

    # [U] ALL -- catch-all, used by deny policies
    edit "ALL"
        set protocol IP
        set comment "ALL protocols - equivalent to PAN any service"
    next

end


# =============================================================================
# SECTION 10d: FIREWALL SERVICE GROUPS
# Equivalent to PAN: service-group objects
#
# USED-BY legend:  [U] = referenced by a policy   [O] = orphan (never used)
# =============================================================================
config firewall service group

    # ------------------------------------------------------------------
    # LEAF service groups -- contain only service objects
    # ------------------------------------------------------------------

    # [U] Web_Services_Grp -- used by Policies 2, 4, 8, 11 (service field)
    #     name has DOT -- ILLEGAL for Versa
    edit "Web.Services.Group"
        set member "Custom_HTTP" "Custom_HTTPS" "Custom.HTTP-8080" "App:Server 8443" "HighPorts Range[TCP]"
        set comment "ILLEGAL NAME: dots -- all web/HTTP service objects"
    next

    # [U] DB_Services_Grp -- referenced in policies for database access
    #     name has SLASH -- ILLEGAL for Versa
    edit "DB/Services-Group"
        set member "Oracle DB Listener" "SAP/HANA-Port" "MSSQL @ DB Range"
        set comment "ILLEGAL NAME: slash -- database service objects"
    next

    # [U] Remote_Access_Grp -- referenced in policies for remote access
    #     name has PARENS -- ILLEGAL for Versa
    edit "Remote_Access(All)"
        set member "Custom RDP(TS)" "SSH [Admin]" "VNC Range(Multi)" "PCoIP.VMware Horizon"
        set comment "ILLEGAL NAME: parens -- all remote access protocols"
    next

    # [U] Mail_Services_Grp -- referenced in mail relay policies
    #     name has AMPERSAND+SPACE -- ILLEGAL for Versa
    edit "Mail & Messaging Svcs"
        set member "SMTP & Submission" "IMAP.Secure" "Custom_DNS"
        set comment "ILLEGAL NAME: ampersand+space -- email service objects"
    next

    # [G] Mgmt_Services_Grp -- used inside parent group All_Infra_Services
    #     name has HASH -- ILLEGAL for Versa
    edit "Mgmt#Services-Grp"
        set member "SNMP#Monitor" "SSH [Admin]" "Mgmt Ports+Range"
        set comment "ILLEGAL NAME: hash -- management protocol services"
    next

    # [G] IoT_Services_Grp -- used inside parent group All_Infra_Services
    #     name has AT -- ILLEGAL for Versa
    edit "IoT@Device-Services"
        set member "MQTT@IoT" "Dynamic/UDP Range" "Voice RTP+Ports"
        set comment "ILLEGAL NAME: at-sign -- IoT and media streaming services"
    next

    # ------------------------------------------------------------------
    # PARENT service groups (group-in-group)
    # ------------------------------------------------------------------

    # [U] All_Infra_Services -- used by Policy 1 (RAVPN-intern-High-Bandwidth)
    #     Contains: 2 leaf service objects + 2 child service groups (group-in-group)
    #     name has PLUS+SPACE -- ILLEGAL for Versa
    edit "All Infra+Services"
        set member "Custom_DNS" "ALL" "Mgmt#Services-Grp" "IoT@Device-Services"
        set comment "ILLEGAL NAME: plus+space -- parent group: basic svcs + infra child groups"
    next

    # [U] Full_Remote_Access -- used by Policy 13 (long-name policy, service field)
    #     Contains: 1 service object + 1 child group (group-in-group)
    #     name has BRACKETS+AT -- ILLEGAL for Versa
    edit "Full[Remote]@Access"
        set member "ALL" "Remote_Access(All)"
        set comment "ILLEGAL NAME: brackets+at -- parent group: ALL + remote-access child group"
    next

    # ------------------------------------------------------------------
    # Orphan service groups -- not referenced by any policy
    # ------------------------------------------------------------------

    # [O] Valid name -- ORPHAN
    edit "Orphan_Service_Group"
        set member "Custom_HTTP" "Custom_HTTPS" "Custom_DNS"
        set comment "ORPHAN: valid name -- basic web group, not referenced by any policy"
    next

    # [O] Name has COLON+SPACE -- ILLEGAL + orphan
    edit "Legacy: App Services"
        set member "legacy.app.port" "Orphan_TCP_Service"
        set comment "ILLEGAL NAME: colon+space -- ORPHAN: legacy services not used anywhere"
    next

    # [O] Name has HASH+BRACKETS+SPACE -- ILLEGAL + orphan, group-in-group
    edit "Unused#Group[Nested]"
        set member "Web.Services.Group" "Orphan_Service_Group"
        set comment "ILLEGAL NAME: hash+brackets+space -- ORPHAN nested group, not used anywhere"
    next

end


# SECTION 10: FIREWALL POLICIES WITH FULL SECURITY PROFILES
# Equivalent to PAN security rules with profile-setting (url-filtering,
# virus, spyware, vulnerability)
# This is the key section - policies referencing security profiles
# -----------------------------------------------------------------------------
config firewall policy
    # --- Policy 1: VPN Users to Internet with full security profiles ---
    # PAN equivalent:
    #   set profile-setting profiles url-filtering Shore_Strict_Allow
    #   set profile-setting profiles virus AV_drop_default
    #   set profile-setting profiles vulnerability IPS_drop_default
    edit 1
        set name "RAVPN-intern-High-Bandwidth"
        set srcintf "ssl.root" "wan2"
        set dstintf "wan1"
        set srcaddr "ravpn-client-net" "WebServer_DMZ" "All_Servers_Grp"
        set dstaddr "Server_Farm_Primary" "LAN" "api.vendor.prod"
        set action accept
        set schedule "always"
        set service "Dynamic/UDP Range" "DB/Services-Group"
        set groups "FIM-TECH-Entertainment-Group" "v.IT-System Admins"
        set users "Karsten Kubeler" "hallidri.refat"
        set utm-status enable
        set inspection-mode proxy
        set url-category "Malicious-Websites" "Hacking" "Phishing" "Blocked_Static_URLs"
        set webfilter-profile "Shore_Strict_Allow"
        set av-profile "AV_drop_default"
        set ips-sensor "IPS_drop_default"
        set application-list "App_Control_Standard" "App_Control_Strict"
        set dnsfilter-profile "DNS_Filter_Default"
        set ssl-ssh-profile "deep-inspection"
        set logtraffic all
        set logtraffic-start enable
        set log-http-transaction enable
        set nat enable
        set comments "Allow most dstintf unknown srcintf unknown Internet traffic for VPN intern users - equiv PAN RAVPN-PRE rule"
    next

    edit 2
        set name "Unknown URLF"
        set srcintf "ssl.root" "remote"
        set dstintf "wan1" "wan2"
        set srcaddr "ravpn-client-net" "PUBLIC-NETWORKS"
        set dstaddr "WebServer_DMZ" "Internal_WebServer"
        set action accept
        set schedule "always"
        set service "Custom_HTTP" "Custom_HTTPS" "Mgmt#Services-Grp"
        set groups "FIM-TECH-Entertainment-Group" "v.IT-System Admins"
        set utm-status enable
        set inspection-mode proxy
        set application-list "App_Control_Standard"
        set url-category "Pornography" "Gambling" "Drug-Abuse"
        set webfilter-profile "Shore_Strict_Allow" "unknown_urlf"
        set av-profile "AV_drop_default"
        set ips-sensor "IPS_drop_default"
        set ssl-ssh-profile "deep-inspection"
        set logtraffic all
        set nat enable
        set comments "LDAP TEST: mixed srcintf user types -- LEGAL + ILLEGAL-dot + ILLEGAL-space all in one set users"
    next

    edit 3
        set name "Uknown Addr Grp"
        set srcintf "ssl.root" "remote"
        set dstintf "wan1" "wan2"
        set srcaddr "ravpn-client-net" "PUBLIC-NETWORKS" "unknown-addr"
        set dstaddr "WebServer_DMZ" "Internal_WebServer"
        set action accept
        set schedule "always"
        set service "Custom_HTTP" "Custom_HTTPS" "Mgmt#Services-Grp"
        set groups "FIM-TECH-Entertainment-Group" "v.IT-System Admins"
        set utm-status enable
        set inspection-mode proxy
        set application-list "App_Control_Standard"
        set url-category "Pornography" "Gambling" "Drug-Abuse"
        set webfilter-profile "Shore_Strict_Allow"
        set av-profile "AV_drop_default"
        set ips-sensor "IPS_drop_default"
        set ssl-ssh-profile "deep-inspection"
        set logtraffic all
        set nat enable
        set comments "LDAP TEST: mixed srcintf user types -- LEGAL + ILLEGAL-dot + ILLEGAL-space all in one set users"
    next

    edit 4
        set name "Unknown User"
        set srcintf "ssl.root" "remote"
        set dstintf "wan1" "wan2"
        set srcaddr "ravpn-client-net" "PUBLIC-NETWORKS" 
        set dstaddr "WebServer_DMZ" "Internal_WebServer"
        set action accept
        set schedule "always"
        set service "Custom_HTTP" "Custom_HTTPS" "Mgmt#Services-Grp"
        set groups "FIM-TECH-Entertainment-Group" "v.IT-System Admins"
        set users "unknown_user" "perkins.derek"
        set utm-status enable
        set inspection-mode proxy
        set application-list "App_Control_Standard"
        set url-category "Pornography" "Gambling" "Drug-Abuse"
        set webfilter-profile "Shore_Strict_Allow"
        set av-profile "AV_drop_default"
        set ips-sensor "IPS_drop_default"
        set ssl-ssh-profile "deep-inspection"
        set logtraffic all
        set nat enable
        set comments "LDAP TEST: mixed srcintf user types -- LEGAL + ILLEGAL-dot + ILLEGAL-space all in one set users"
    next

    edit 5
        set name "Unknown Group"
        set srcintf "ssl.root" "remote"
        set dstintf "wan1" "wan2"
        set srcaddr "ravpn-client-net" "PUBLIC-NETWORKS" 
        set dstaddr "WebServer_DMZ" "Internal_WebServer"
        set action accept
        set schedule "always"
        set service "Custom_HTTP" "Custom_HTTPS" "Mgmt#Services-Grp"
        set groups "unknown_group" "FIM-TECH-Entertainment-Group" "v.IT-System Admins"
        set users "unknown_user" "perkins.derek"
        set utm-status enable
        set inspection-mode proxy
        set application-list "App_Control_Standard"
        set url-category "Pornography" "Gambling" "Drug-Abuse"
        set webfilter-profile "Shore_Strict_Allow"
        set av-profile "AV_drop_default"
        set ips-sensor "IPS_drop_default"
        set ssl-ssh-profile "deep-inspection"
        set logtraffic all
        set nat enable
        set comments "LDAP TEST: mixed srcintf user types -- LEGAL + ILLEGAL-dot + ILLEGAL-space all in one set users"
    next

   


end


# =============================================================================
# REFERENCE: KEY CONCEPT DIFFERENCES vs PAN
#
# PAN profile-setting in rule:          FortiGate equivalent in policy:
# -----------------------------------------
# profile-setting profiles url-filtering <name>   ->  webfilter-profile "<name>"
# profile-setting profiles virus <name>            ->  av-profile "<name>"
# profile-setting profiles spyware <name>          ->  ips-sensor "<name>"  (partial)
# profile-setting profiles vulnerability <name>    ->  ips-sensor "<name>"
# profile-setting group <group-name>               ->  profile-group "<name>" (FortiGate profile-group)
# application [...list...]                         ->  application-list "<name>" OR set application <ids>
# source-user <user\group>                         ->  set groups "<group>" OR set users "<user>"
# log-setting Panorama                             ->  set logtraffic all + FortiAnalyzer config
# tag <value>                                      ->  set comments (no native tag on policy)
#
# PAN URL-filter profile fields:        FortiGate webfilter profile fields:
# -----------------------------------------
# allow [url-list]                     ->  urlfilter entries with action allow
# block [url-list]                     ->  urlfilter entries with action block
# alert [category-list]                ->  ftgd-wf filters with action monitor
# block [named-categories]             ->  ftgd-wf filters with action block
# credential-enforcement               ->  antiphish config block (proxy mode only)
# =============================================================================